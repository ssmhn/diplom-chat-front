export type Props = {
    distance?: number,
    durationIn?: number,
    durationOut?: number,
    easeIn?: number[],
    easeOut?: number[],
    duration?: number,
    ease?: number[],
    staggerIn?: number,
    colors?: string[]
}